process FinalFit = 
{

  # initialize  MessageLogger
  include "FWCore/MessageLogger/data/MessageLogger.cfi"
 
  # initialize magnetic field
  include "MagneticField/Engine/data/volumeBasedMagneticField.cfi"

  # geometry (switched to tracker only geometry)
  # include "Geometry/CMSCommonData/data/cmsIdealGeometryXML.cfi"
  es_source = XMLIdealGeometryESSource 
  {
    FileInPath GeometryConfiguration="Geometry/TrackerSimData/data/trackerSimConfiguration.xml"
    string rootNodeName = ""
  }

  # tracker geometry
  include "Geometry/TrackerGeometryBuilder/data/trackerGeometry.cfi"

  # tracker numbering
  include "Geometry/TrackerNumberingBuilder/data/trackerNumberingGeometry.cfi"

  source = PoolSource 
  { 
    untracked vstring fileNames = {"file:SeedsToTrackCandidates.root"}
    untracked int32 maxEvents = -1
  }

  # TrackProducer with material
  include "RecoTracker/TrackProducer/data/FinalFitWithMaterial.cfi"
  replace TrackProducer.src = "trackCandidateMaker"

  # second instance for RS
  module RoadSearchTrackProducer = TrackProducer 
  {
    string Fitter = "KFFittingSmoother"   
    string Propagator = "PropagatorWithMaterial" 
    string src = "RoadSearchTrackCandidates"
  }                                                                                                                                                           

  module AsciiOutput = AsciiOutputModule {}

  module out = PoolOutputModule 
  {
    untracked string fileName = "TrackCandidatesToTracks.root"
  }

  path p = 
  {
    TrackProducer,
    RoadSearchTrackProducer
  }
  
  endpath e = 
  {
    AsciiOutput, out
  }
}
